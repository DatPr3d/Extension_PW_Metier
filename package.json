{
  "name": "scenario-capture-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Extension Chrome/Edge interne pour capturer des scénarios métier et exporter un brouillon Playwright.",
  "scripts": {
    "check": "node --check src/background.js && node --check src/content-recorder.js && node --check src/popup.js"
  }
}
